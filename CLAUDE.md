# CLAUDE.md

@AGENTS.md

## Always-loaded context

@PRODUCT.md
@ARCHITECTURE.md

## Load when needed

- UI work: read `DESIGN.md` first. If it doesn't exist, use the `design` skill.
- User-facing flows: read `USER-JOURNEY.md`.
- Feature work: read the matching folder in `specs/`, or use the `spec` skill to create one.

## Skills

This repo uses the `your-toolkit` plugin (`product`, `design`, `architecture`, `user-journey`, `spec`, `motion`). Keep `PRODUCT.md`, `ARCHITECTURE.md`, and `DESIGN.md` up to date with those skills when things change.
